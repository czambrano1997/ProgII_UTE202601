<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Form View -->
        <record id="cliente_view_form" model="ir.ui.view">
            <field name="name">cliente.view.form</field>
            <field name="model">cliente.view</field>
            <field name="arch" type="xml">
                <form string="Cliente View">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <!-- Add your fields here -->
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
        
        <!-- List View -->
        <record id="cliente_view_list" model="ir.ui.view">
            <field name="name">cliente.view.list</field>
            <field name="model">cliente.view</field>
            <field name="arch" type="xml">
                <list string="Cliente View">
                    <field name="name"/>
                    <!-- Add your fields here -->
                </list>
            </field>
        </record>
        
        <!-- Action -->
        <record id="action_cliente_view" model="ir.actions.act_window">
            <field name="name">Cliente View</field>
            <field name="res_model">cliente.view</field>
            <field name="view_mode">list,form</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create your first cliente view!
                </p>
            </field>
        </record>
        
        <!-- Menu -->
        <menuitem id="menu_cliente_view"
                  name="Cliente View"
                  action="action_cliente_view"
                  sequence="10"/>
    </data>
</odoo>