<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Form View -->
        <record id="inventario_view_form" model="ir.ui.view">
            <field name="name">inventario.view.form</field>
            <field name="model">inventario.view</field>
            <field name="arch" type="xml">
                <form string="Inventario View">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <!-- Add your fields here -->
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
        
        <!-- List View -->
        <record id="inventario_view_list" model="ir.ui.view">
            <field name="name">inventario.view.list</field>
            <field name="model">inventario.view</field>
            <field name="arch" type="xml">
                <list string="Inventario View">
                    <field name="name"/>
                    <!-- Add your fields here -->
                </list>
            </field>
        </record>
        
        <!-- Action -->
        <record id="action_inventario_view" model="ir.actions.act_window">
            <field name="name">Inventario View</field>
            <field name="res_model">inventario.view</field>
            <field name="view_mode">list,form</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create your first inventario view!
                </p>
            </field>
        </record>
        
        <!-- Menu -->
        <menuitem id="menu_inventario_view"
                  name="Inventario View"
                  action="action_inventario_view"
                  sequence="10"/>
    </data>
</odoo>