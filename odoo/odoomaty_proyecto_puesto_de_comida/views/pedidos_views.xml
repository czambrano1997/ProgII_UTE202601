<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Form View -->
        <record id="pedidos_view_form" model="ir.ui.view">
            <field name="name">pedidos.view.form</field>
            <field name="model">pedidos.view</field>
            <field name="arch" type="xml">
                <form string="Pedidos View">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <!-- Add your fields here -->
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
        
        <!-- List View -->
        <record id="pedidos_view_list" model="ir.ui.view">
            <field name="name">pedidos.view.list</field>
            <field name="model">pedidos.view</field>
            <field name="arch" type="xml">
                <list string="Pedidos View">
                    <field name="name"/>
                    <!-- Add your fields here -->
                </list>
            </field>
        </record>
        
        <!-- Action -->
        <record id="action_pedidos_view" model="ir.actions.act_window">
            <field name="name">Pedidos View</field>
            <field name="res_model">pedidos.view</field>
            <field name="view_mode">list,form</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create your first pedidos view!
                </p>
            </field>
        </record>
        
        <!-- Menu -->
        <menuitem id="menu_pedidos_view"
                  name="Pedidos View"
                  action="action_pedidos_view"
                  sequence="10"/>
    </data>
</odoo>